# virtualenv
.venv/
__pycache__/
*.pyc

# cached scraped HTML (regenerable by the extractors)
*.html

# crawl resume state (regenerable; large)
data/*_checkpoint.json

# generated/scraped data (regenerate via the extractors); keep research inputs + the CSV deliverable tracked
data/*.parquet
all_five.md
